# Firefox Workspace Integration
# Window rules for Firefox browser windows

# Basic Firefox window rules
windowrulev2 = float, class:^(firefox)$, title:^(Library)$
windowrulev2 = float, class:^(firefox)$, title:^(Firefox — Sharing Indicator)$
windowrulev2 = float, class:^(firefox)$, title:^(Picture-in-Picture)$

# Allow Firefox to move between workspaces freely
# (The workspace daemon will handle automatic movement)
windowrulev2 = workspace unset, class:^(firefox)$

# Ensure Firefox windows are not pinned to specific workspaces
# This allows the workspace switcher to move them dynamically
